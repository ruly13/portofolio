{
  "name": "resume-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix package",
    "build": "npm run build --prefix package",
    "start": "npm run start --prefix package",
    "lint": "npm run lint --prefix package"
  },
  "dependencies": {
    "@vercel/speed-insights": "^1.3.1"
  }
}
